# 🧪 Тестування системи управління користувачами

Цей файл містить інструкції та приклади для тестування всіх функцій системи.

## 1. Стартова перевірка

### Крок 1: Встановлення залежностей

```bash
pip install -r requirements.txt
```

### Крок 2: Запуск додатку

```bash
python run.py
```

Додаток буде доступний за адресою: **http://localhost:5000**

### Крок 3: Створення першого адміністратора

```bash
python create_admin.py
```

Слідуйте інструкціям на екрані.

---

## 2. Тестування функцій аутентифікації

### 2.1 Реєстрація нового користувача (Technician)

**URL:** http://localhost:5000/auth/register

**Шаги:**
1. Клік на "Реєстрація" у головній навігації
2. Заповніть форму:
   - Username: `tech_test`
   - Email: `tech@test.local`
   - Password: `test1234567`
   - Confirm Password: `test1234567`
3. Клік на "Зареєструватися"

**Очікуваний результат:**
- ✅ Форма має пройти валідацію
- ✅ Користувач має бути створено з роллю "technician"
- ✅ Перенаправлення на сторінку логіну
- ✅ Flash повідомлення: "Реєстрація успішна! Тепер ви можете увійти."

### 2.2 Перевірка валідації форми реєстрації

**Тест 1: Коротке ім'я користувача**
- Username: `ab`
- Очікуваний результат: ❌ "Ім'я користувача повинно бути від 3 до 20 символів"

**Тест 2: Невалідний email**
- Email: `invalid-email`
- Очікуваний результат: ❌ "Невалідна адреса email"

**Тест 3: Різні паролі**
- Password: `test1234567`
- Confirm Password: `different`
- Очікуваний результат: ❌ "Паролі повинні співпадати"

**Тест 4: Короткий пароль**
- Password: `short`
- Очікуваний результат: ❌ "Пароль повинен містити мінімум 8 символів"

**Тест 5: Дублювання username**
- Username: `admin` (якщо уже існує)
- Очікуваний результат: ❌ "Ім'я користувача вже існує"

### 2.3 Логін користувача

**URL:** http://localhost:5000/auth/login

**Тест 1: Успішний логін**
- Username: `tech_test`
- Password: `test1234567`
- Remember: (галочка опціонально)
- Очікуваний результат:
  - ✅ Перенаправлення на Dashboard
  - ✅ Username у навігації показує "tech_test"
  - ✅ Доступна кнопка "Профіль"

**Тест 2: Невірний пароль**
- Username: `tech_test`
- Password: `wrong_password`
- Очікуваний результат:
  - ❌ Flash повідомлення: "Невірне ім'я користувача або пароль"
  - ❌ Залишитися на сторінці логіну

**Тест 3: Невірне ім'я користувача**
- Username: `nonexistent`
- Password: `any_password`
- Очікуваний результат:
  - ❌ Flash повідомлення: "Невірне ім'я користувача або пароль"

### 2.4 Профіль користувача

**URL:** http://localhost:5000/auth/profile

**Тест 1: Перегляд даних профілю**
- Очікувані дані:
  - Username: показується коректне ім'я
  - Email: показується коректна адреса
  - Роль: показується "Технік" (для tech_test)
  - Статус: показується "Активний"
  - Дата реєстрації: показується дата/час
  - Останній вхід: показується дата/час

**Тест 2: Зміна пароля**
1. Введіть поточний пароль: `test1234567`
2. Введіть новий пароль: `newpass1234`
3. Підтвердіть новий пароль: `newpass1234`
4. Клік "Змінити пароль"

Очікуваний результат:
- ✅ Flash повідомлення: "Пароль успішно змінений"
- ✅ Логін повинен працювати з новим паролем
- ✅ Логін повинен НЕ працювати зі старим паролем

### 2.5 Логаут

**URL:** http://localhost:5000/auth/logout

- Клік на "Вихід" у профіль-меню
- Очікуваний результат:
  - ✅ Перенаправлення на сторінку логіну
  - ✅ Flash повідомлення: "Ви вийшли з системи"
  - ✅ Навігація змінюється на "Вхід | Реєстрація"

---

## 3. Тестування адмін-панелі

### 3.1 Доступ до адмін-панелі

**Як адміністратор:**
1. Логініться з обліком admin
2. URL: http://localhost:5000/admin/

Очікуваний результат:
- ✅ Доступна адмін-панель
- ✅ Показуються статистика:
  - Всього користувачів
  - Активних користувачів
  - Кількість адміністраторів
  - Кількість техніків
  - Кількість операторів

**Як технік:**
1. Логініться з обліком tech_test
2. URL: http://localhost:5000/admin/
- ❌ Flash повідомлення: "Ви не маєте доступу до цієї сторінки"
- ❌ Перенаправлення на Dashboard

### 3.2 Список користувачів

**URL:** http://localhost:5000/admin/users

**Тест 1: Перегляд списку**
- Очікувані колонки:
  - Ім'я користувача
  - Email
  - Роль (badge з кольором)
  - Статус (Активний/Неактивний)
  - Останній вхід
  - Дії (кнопки)

**Тест 2: Фільтрація та сортування**
- Користувачі повинні бути відсортовані за датою створення (новіші перші)

### 3.3 Додавання користувача адміністратором

**URL:** http://localhost:5000/admin/users/add

**Шаги:**
1. Клік "Додати користувача"
2. Заповніть форму:
   - Username: `operator_test`
   - Email: `operator@test.local`
   - Password: `op123456789`
   - Role: "Оператор"
3. Клік "Створити користувача"

Очікуваний результат:
- ✅ Flash повідомлення: "Користувач operator_test успішно створено"
- ✅ Перенаправлення на список користувачів
- ✅ Новий користувач видимий у списку з роллю "Оператор"

### 3.4 Редагування користувача

**URL:** http://localhost:5000/admin/users/<id>/edit

**Тест 1: Зміна email та ролі**
1. Клік на користувача (напр. operator_test)
2. Змініть:
   - Email: `new_op@test.local`
   - Role: "Технік"
3. Клік "Зберегти зміни"

Очікуваний результат:
- ✅ Flash повідомлення: "Користувач успішно оновлено"
- ✅ Дані відновлені у списку

**Тест 2: Зміна пароля користувача**
1. Заповніть поле "Новий пароль": `reset123456`
2. Клік "Зберегти зміни"
3. Користувач повинен мати можливість логінитися з новим паролем

### 3.5 Блокування користувача

**URL:** http://localhost:5000/admin/users

**Шаги:**
1. Знайдіть користувача у списку (напр. operator_test)
2. Клік "Деактивувати"
3. Підтвердіть в діалозі

Очікуваний результат:
- ✅ Flash повідомлення: "Користувач деактивовано"
- ✅ Статус змінюється на "Неактивний"
- ✅ Користувач НЕ може логінитися (повідомлення: "Ваш акаунт деактивований")

**Активація:**
- Клік "Активувати" повинен повернути доступ

### 3.6 Видалення користувача

**URL:** http://localhost:5000/admin/users

**Шаги:**
1. Клік на кнопку "Видалити" біля користувача
2. Підтвердіть у діалозі

Очікуваний результат:
- ✅ Flash повідомлення: "Користувач видалено"
- ✅ Користувач НЕ видимий у списку
- ✅ Користувач НЕ може логінитися

**Захист:**
- ❌ Не можете видалити себе (повідомлення: "Ви не можете видалити свій облік")

---

## 4. Тестування прав доступу

### 4.1 Перевірка доступу до маршрутів

**Адміністратор:**
- ✅ /admin/ - доступ
- ✅ /admin/users - доступ
- ✅ /admin/users/add - доступ
- ✅ /auth/profile - доступ
- ✅ Всі інші маршрути - доступ

**Технік:**
- ❌ /admin/ - 403 Forbidden (перенаправлення)
- ❌ /admin/users - 403 Forbidden
- ✅ /auth/profile - доступ
- ✅ Аплікатори - доступ
- ✅ Історія - доступ

**Оператор:**
- ❌ /admin/ - 403 Forbidden
- ✅ /auth/profile - доступ
- ✅ Dashboard - доступ (обмежено)

---

## 5. Тестування безпеки

### 5.1 CSRF захист

**Тест 1: Відправка форми без CSRF токену**
- Видаліть 'csrf_token' з HTML форми
- Очікуваний результат: ❌ 400 Bad Request

**Тест 2: Коректна форма**
- CSRF токен присутній
- Очікуваний результат: ✅ 200 OK

### 5.2 Хешування паролів

1. Подивіться на дані користувача (напр. у data/users.json)
2. Пароль повинен бути в хеш-форматі
3. Оригінальний пароль НЕ видимий у файлі

### 5.3 Сесійні куки

- Відкрийте DevTools (F12)
- Перейдіть на вкладку "Application" → "Cookies"
- Знайдіть сесійну куку
- Властивості:
  - ✅ HttpOnly: true (JS не може читати)
  - ✅ Secure: залежить від HTTPS (для production)
  - ✅ SameSite: Lax (CSRF захист)

---

## 6. Тестування демо облікових записів

Якщо у системі є демо облікові записи, протестуйте кожен з них:

**Admin (admin / admin123)**
- ✅ Логін
- ✅ Доступ до адмін-панелі
- ✅ Вихід

**Technician (tech1 / tech123)**
- ✅ Логін
- ✅ Доступ заборонено до адмін-панелі
- ✅ Доступ до аплікаторів

**Operator (operator1 / op123)**
- ✅ Логін
- ✅ Доступ заборонено до адмін-панелі
- ✅ Dashboard доступний (обмежено)

---

## 7. Тестування у браузері (вручну)

### Сценарій 1: Повна реєстрація → Логін → Профіль

1. Відкрити http://localhost:5000
2. Клік на "Реєстрація"
3. Заповнити форму (новий користувач)
4. Успішна реєстрація
5. Логін з новими обліковими даними
6. Доступ до профілю
7. Зміна пароля
8. Логаут

### Сценарій 2: Адміністративне управління

1. Логін як admin
2. Перейти на /admin/users
3. Створити нового користувача
4. Редагувати користувача
5. Блокувати користувача
6. Видалити користувача
7. Логаут

### Сценарій 3: Перевірка доступу

1. Логін як technician
2. Спробувати доступ до /admin/
3. ✅ Перенаправлення з помилкою
4. Логаут

---

## 8. Перевірка логів

Дивіться консоль Flask для логів:
- Запити користувача
- Помилки валідації
- Дії адміністратора

---

## 9. Результат тестування

Якщо всі вищезазначені тести пройшли успішно:
- ✅ Система управління користувачами працює коректно
- ✅ Аутентифікація работает  
- ✅ Права доступу захищені
- ✅ Безпека форм гарантована
- ✅ CSRF захист активний

---

**Дата останнього тестування:** 2026-06-06
**Статус:** Ready for QA
